# NEXORA Nginx / Reverse Proxy Configuration

Production reverse-proxy configuration (TLS termination, routing to
Control Center / Core API / AI Service, rate limiting at the edge) is
built out in **Phase 12 (Deployment)**, once there are multiple production
services to route between.
