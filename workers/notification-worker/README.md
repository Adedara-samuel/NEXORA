# NEXORA Notification Worker

BullMQ + Redis background worker for email, SMS, in-app and desktop
notification delivery via provider adapters (architecture sections 65, 83).

**Not started.** First needed in **Phase 2** (organisation invitation
emails) and expanded through every subsequent phase as new notification
triggers are added.
